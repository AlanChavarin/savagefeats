// beforeEach(() => {
//     // restores database before each test runs 
//     const resetMongoDBCommand = (`mongorestore --drop --uri ${Cypress.env('MONGO_TEST_URI')} ../mongodump`)
//     cy.exec(resetMongoDBCommand)
// })

// context("postReport test suite", () => {
//     it("postReport", () => {
        
//     })
// })